[project]
name = "wave-pool-weather"
version = "0.1.0"
description = "Wave pool weather monitoring"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
